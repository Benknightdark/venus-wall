apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mdk-worker.fullname" . }}-test-connection"
  labels:
    {{- include "mdk-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mdk-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
